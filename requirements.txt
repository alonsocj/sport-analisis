# Stack del proyecto (Python 3.11+). Pins de piso razonables.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
statsmodels>=0.14
requests>=2.31
pydantic-settings>=2.0
pytest>=7.4
plotly>=5
# matplotlib eliminado 2026-06-10: cero usos en src/ y tests/ tras retirar src/report/ (dashboard HTML)
# Feature 9 (app_streamlit) — dictamen security-lead 2026-06-09: streamlit>=1.58
# (CVEs graves solo-Windows; Tornado eliminado desde 1.57). pyarrow piso explícito
# >=14.0.1 por CVE-2023-47248 (el constraint >=7.0 de streamlit es insuficiente).
streamlit>=1.58.0
pyarrow>=14.0.1
# Feature 14/15 (scraping fuentes públicas free) — dictamen security-lead 2026-06-17:
# lxml>=6.1.1 corrige XXE (GHSA-vfmq-68hx-4jfw / PYSEC-2026-87; default resolve_entities
# cambió en 6.1.0). Uso obligatorio con parser seguro (no_network=True, resolve_entities=
# False, huge_tree=False). beautifulsoup4 NO se instala salvo que lxml resulte insuficiente
# para Wikipedia (queda como deuda condicional, pin futuro >=4.12.3 + soupsieve>=2.5).
lxml>=6.1.1
